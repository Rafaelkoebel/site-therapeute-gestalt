<header class="site-header">
    <div class="container">

        <div class="header-inner">
            {# Logo / Nom #}
            <div class="site-branding">
                <a href="{{ path('home') }}" class="site-title">
                    Thérapeute Gestalt
                </a>
            </div>

            {# Navigation principale #}
            <nav class="main-navigation" aria-label="Navigation principale">
                <ul class="nav-list">
                    <li>
                        <a href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li>
                        <a href="{{ path('page_show', { slug: 'qui-je-suis' }) }}">
                            Qui je suis
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('page_show', { slug: 'gestalt-therapie' }) }}">
                            La Gestalt-thérapie
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('page_show', { slug: 'pour-qui' }) }}">
                            Pour qui
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('page_show', { slug: 'infos-pratiques' }) }}">
                            Infos pratiques
                        </a>
                    </li>
                    <li class="nav-contact">
                        <a href="{{ path('contact') }}" class="btn-contact">
                            Prendre rendez-vous
                        </a>
                    </li>
                </ul>
            </nav>

            {# Bouton menu mobile #}
            <button class="menu-toggle"
                    aria-label="Ouvrir le menu"
                    aria-expanded="false">
                ☰
            </button>
        </div>

    </div>
</header>
